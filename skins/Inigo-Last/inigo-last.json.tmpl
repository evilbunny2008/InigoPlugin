#errorCatcher Echo
#encoding UTF-8
##
##
#from Cheetah import NameMapper
##
##
##
#set $inigoData = {"version": 1000000, "processingErrors": []}
##
##
#try
    #set $inigoData["report_time"] = $yesterday.start.raw + 86400
    ##
    ##
    ##
    #set $vars = ["outTemp", "inTemp", "outHumidity", "inHumidity", "barometer", "dewpoint"]
    #set $time_periods = ["yesterday", "last_month", "last_year"]
    ##
    ##
    ##
    #try
        #for $var in $vars
            #for $time_period in $time_periods
                #set $var_name_str = $time_period + "_" + $var
                #set $var_name = getattr($getVar($time_period), $var, None)
                #if $var_name is not None and $var_name.has_data
                    #set $inigoData[$var_name_str + "_max"] = $raw_value($var_name.max, $var)
                    #set $inigoData[$var_name_str + "_min"] = $raw_value($var_name.min, $var)
                    #set $inigoData[$var_name_str + "_maxtime"] = $raw_value($var_name.maxtime, "unix_epoch")
                    #set $inigoData[$var_name_str + "_mintime"] = $raw_value($var_name.mintime, "unix_epoch")
                #end if
            #end for
        #end for
    #except
        #set $exc_info = $sys.exc_info()
        #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
        #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
    #end try
    ##
    ##
    ##
    #try
        #set $var = "wind"
        #for $time_period in $time_periods
            #set $var_name_str = $time_period + "_" + $var
            #set $var_name = getattr($getVar($time_period), $var, None)
            #if $var_name is not None and $var_name.has_data
                #set $inigoData[$var_name_str + "_avg"] = $raw_value($var_name.avg, $var)
                #set $inigoData[$var_name_str + "_max"] = $raw_value($var_name.max, $var)
                #set $inigoData[$var_name_str + "_maxdir"] = $raw_value($var_name.gustdir, "degree_compass")
                #set $inigoData[$var_name_str + "_maxdir_compass"] = $var_name.gustdir.ordinal_compass
                #set $inigoData[$var_name_str + "_maxtime"] = $raw_value($var_name.maxtime, "unix_epoch")
                #set $inigoData[$var_name_str + "_vecdir"] = $raw_value($var_name.vecdir, "degree_compass")
                #set $inigoData[$var_name_str + "_vecdir_compass"] = $var_name.vecdir.ordinal_compass
            #end if
        #end for
    #except
        #set $exc_info = $sys.exc_info()
        #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
        #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
    #end try
    ##
    ##
    ##
    #try
        #set $vars = ["ET", "rain"]
        #for $var in $vars
            #for $time_period in $time_periods
                #set $var_name = getattr($getVar($time_period), $var, None)
                #if $var_name is not None and $var_name.has_data
                    #set $inigoData[$time_period + "_" + $var + "_sum"] = $raw_value($var_name.sum, $var)
                #end if
            #end for
        #end for
    #except
        #set $exc_info = $sys.exc_info()
        #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
        #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
    #end try
    ##
    ##
    ##
    #try
        #set $vars = ["radiation", "UV"]
        #for $var in $vars
            #for $time_period in $time_periods
                #set $var_name_str = $time_period + "_" + $var
                #set $var_name = getattr($getVar($time_period), $var, None)
                #if $var_name is not None and $var_name.has_data
                    #set $inigoData[$var_name_str + "_max"] = $raw_value($var_name.max, $var)
                    #set $inigoData[$var_name_str + "_maxtime"] = $raw_value($var_name.maxtime, "unix_epoch")
                #end if
            #end for
        #end for
    #except
        #set $exc_info = $sys.exc_info()
        #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
        #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
    #end try
    ##
    ##
    ##
#except NameMapper.NotFound
    #set $exc_info = $sys.exc_info()
    #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
    #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
#except
    #set $exc_info = $sys.exc_info()
    #set $exc_type_str = $str($exc_info[0].__name__) + ": " + $str($exc_info[1])
    #set $inigoData["processingErrors"] += ["failed at block 1, var: " + $var + ", time_period: " + $time_period + ", " + $exc_type_str]
#end try
##
##
##
#echo $sort_dict($inigoData)
